{
  "name": "nanoflux-react-demo",
  "version": "0.0.1",
  "private": true,
  "homepage": "http://ohager.github.io/nanoflux-react-demo",
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "nanoflux": "latest",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "seamless-immutable": "^7.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build&&gh-pages -d build"
  }
}
